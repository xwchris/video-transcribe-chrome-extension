{
  "name": "video-transcribe-chrome-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension to transcribe public video links with Videosays.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "vite build",
    "audit:remote-code": "node scripts/audit-remote-code.mjs",
    "package": "npm run build && npm run audit:remote-code && node scripts/zip-dist.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "typescript": "^7.0.2",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
